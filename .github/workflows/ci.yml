name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Install Go
        uses: actions/setup-go@v3.2.1
        with:
          go-version: 1.15

      - name: Checkout
        uses: actions/checkout@v3.5.1

      - name: Build
        run: make
