name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3

      - name: Install Go
        uses: actions/setup-go@v4.0.1
        with:
          go-version-file: go.mod

      - name: Build
        run: make
